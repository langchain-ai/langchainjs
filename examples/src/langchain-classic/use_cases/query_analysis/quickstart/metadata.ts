/*
 * Example disabled: this quickstart depended on a local `docs.ts`/`docs.js`
 * helper that is no longer present in the examples package after dependency
 * updates and related cleanup.
 *
 * If this example is restored in the future, reintroduce the companion docs
 * loader module and update the import to match the generated file layout.
 */
