/*
 * Example temporarily disabled.
 *
 * This file previously demonstrated advanced Redis filtering using the legacy
 * RedisVectorStore API. After dependency updates, the advanced filtering flow
 * now belongs on FluentRedisVectorStore and requires a larger example rewrite
 * than is appropriate for a focused type-fix cleanup.
 *
 * If this example is restored, it should be rewritten against the current
 * FluentRedisVectorStore API from "@langchain/redis".
 */
