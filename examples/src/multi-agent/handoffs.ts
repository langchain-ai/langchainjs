/*
 * This example has been commented out because the older handoff pattern no
 * longer type-checks cleanly against the current LangChain/LangGraph state APIs.
 *
 * See `handoffs-customer-support.ts` for a current multi-agent example using
 * the supported middleware and command patterns.
 */
