{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build:extras": {
      "dependsOn": ["@langchain/openai#build", "@langchain/anthropic#build"],
      "outputs": ["dist", "package.json", "tsconfig.json"]
    }
  }
}
