---
"@langchain/typesafe": patch
---

Add `@langchain/typesafe/middleware`, with `modelRouterMiddleware` for TypeSafe `Choice`-based model routing and `autoModeMiddleware` for blocking risky tool calls with a calibrated TypeSafe `Noul` probability.
