---
"@langchain/google": patch
---

Ship declaration maps (`.d.ts.map`, `.d.cts.map`) like the other providers. The
package's tsconfig now matches its siblings; JavaScript and type declarations
are unchanged.
