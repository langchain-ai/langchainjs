---
"@langchain/google": patch
---

fix(google): prevent duplicate functionCall parts, normalize thought content to standard reasoning blocks, and store thoughtSignature in additional_kwargs for proper serialization
