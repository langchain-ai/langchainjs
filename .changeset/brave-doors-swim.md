---
"@langchain/google": minor
---

feat(@langchain/google): support per-invocation additionalHeaders
- Added `additionalHeaders` option to `ChatGoogle` invocation params, allowing custom HTTP headers to be passed on a per-call basis
