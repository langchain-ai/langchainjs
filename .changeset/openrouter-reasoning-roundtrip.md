---
"@langchain/openrouter": patch
---

fix(openrouter): echo `reasoning_details` back on assistant messages, surface mid-stream `error` events as `OpenRouterError`, and honor `disableStreaming` from the constructor
