{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "prebuild": {
      "outputs": []
    },
    "build:scripts": {
      "outputs": ["public/**"],
      "dependsOn": ["prebuild"]
    },
    "build:next": {
      "outputs": [".next/**", ".vercel/**"],
      "dependsOn": ["build:scripts"]
    }
  }
}
