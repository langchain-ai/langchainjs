---
hide_table_of_contents: true
---

import DocCardList from "@theme/DocCardList";

# Custom Agents

See the examples below on how to create custom agents.

The first example uses only a custom prompt prefix and suffix, which is simpler to start. The other two use a completely custom prompt and output parser.

<DocCardList />
