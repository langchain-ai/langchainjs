# Embedbase

Langchain supports using Embedbase database as a vector store.
Refer to the [Embedbase documentation](https://docs.embedbase.xyz) for more information.

## Setup

### Install the library with

```bash npm2yarn
npm install -S embedbase-js
```

## Usage

import CodeBlock from "@theme/CodeBlock";
import Example from "@examples/indexes/vector_stores/embedbase.ts";

<CodeBlock language="typescript">{Example}</CodeBlock>
