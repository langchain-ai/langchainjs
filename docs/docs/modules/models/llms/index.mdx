---
hide_table_of_contents: true
sidebar_label: LLMs
---

import CodeBlock from "@theme/CodeBlock";
import QuickStartExample from "@examples/models/llm/llm_quick_start.ts";
import AdvancedExample from "@examples/models/llm/llm_advanced.ts";
import DocCardList from "@theme/DocCardList";

# Getting Started: LLMs

:::info
[Conceptual Guide](https://docs.langchain.com/docs/components/models/language-model)
:::

LangChain provides a standard interface for using a variety of LLMs.

To get started, simply use the `call` method of an `LLM` implementation, passing in a `string` input. In this example, we are using the `OpenAI` implementation:

<CodeBlock language="typescript">{QuickStartExample}</CodeBlock>

Here's a more advanced example with additional parameters, which sets `-1` for `max_tokens` to turn on token size calculations:

<CodeBlock language="typescript">{AdvancedExample}</CodeBlock>

## Dig deeper

<DocCardList />
