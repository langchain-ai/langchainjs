{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "clean": {
      "dependsOn": ["@langchain/core#build"]
    },
    "build:envs": {
      "dependsOn": ["clean"]
    },
    "build:scripts": {
      "dependsOn": ["build:envs"]
    }
  }
}
