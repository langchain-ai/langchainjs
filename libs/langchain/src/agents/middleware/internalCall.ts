/** Mirrors LangGraph's `TAG_NOSTREAM` (not exported from its package root); keeps internal model calls out of its messages stream. Internal: not re-exported. */
export const INTERNAL_CALL_TAG = "langsmith:nostream";
