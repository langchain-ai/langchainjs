# @langchain/valkey Changelog

## 0.0.1

- Initial release
- Valkey vector store implementation with custom schema support
  - Support for indexed metadata fields (TAG, NUMERIC)
  - Metadata filtering via custom schema
  - Numeric range queries (min/max)
  - TAG field filtering with exact match
